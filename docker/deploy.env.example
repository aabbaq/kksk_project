# Copy to project root on the server as `.env`:
#   cp docker/deploy.env.example .env
#
# Used by: docker compose -f docker/docker-compose.prod.yml

# ── Required ──
JWT_SECRET=replace-with-a-long-random-string-at-least-32-chars

# Public URL users visit (no trailing slash). Used for CORS.
# Example: http://47.xxx.xxx.xxx  or  https://blog.example.com
CORS_ORIGIN=http://YOUR_SERVER_IP_OR_DOMAIN

# ── Image registry (set by CI; override for local builds) ──
IMAGE_REGISTRY=ghcr.io/aabbaq/kksk_project
IMAGE_TAG=latest

# ── HTTP port exposed on the host ──
HTTP_PORT=80

# ── Optional: JWT lifetime ──
JWT_EXPIRES_IN=3h

# ── Optional: Alibaba Cloud OSS (S3-compatible) for image storage ──
# STORAGE_USE_OBJECT_STORE=true
# S3_BUCKET=your-bucket
# S3_REGION=oss-cn-hangzhou
# S3_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_PUBLIC_BASE_URL=https://your-bucket.oss-cn-hangzhou.aliyuncs.com
# S3_KEY_PREFIX=uploads/
# S3_FORCE_PATH_STYLE=false
